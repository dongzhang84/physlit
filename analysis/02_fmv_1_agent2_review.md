# 02_fmv.1 Agent 2 — structural-axis side-resolution review

> Generated by `scripts/build_02_fmv_1_agent2_review.py`. Case-by-case
> review of the 7 structural-axis dual-judge disagreement cases:
> - **Claude / OpenAI structural judges** — the two `02_fmv.1` structural judges.
> - **Agent 2** — `gemini-3.1-pro-preview`, a NON-CANONICAL resolver: it
>   reads both judges' verdicts + the frozen structural criteria and
>   returns PASS/FAIL. Agent 2 does **not** feed P1 / P2 — the human
>   audit (`02_fmv_1_structural_audit_worksheet.md`) is canonical.
> Source JSONs: `results/<model>/02_fmv/structural{,_resolved}/*.json`.

## Headline

**Structural-axis IRR: 7 of 15 trials = 46.67%.** Above v0.2 Aristotelian's 40% — see `02_fmv_1_findings.md` (P1 REFUTED).

**Agent 2 across the 7 disagree cases:** FAIL = 5, PASS = 2.

**Agent 2 sided with:** Claude structural judge on 2/7; OpenAI structural judge on 4/7; neither on 1/7.

---

## K1 — `claude-opus-4-7` trial 2

**Claude structural judge — `PASS`** (Stage 1 rule count `10`, failed: (none))

> Stage 1 contains 10 numbered top-level rules, well under the N9 threshold. The rules are distinct: each covers a different phenomenon (instantaneity, no buildup, proportionality to effort, inverse to heaviness, combination, setting-independence, carrying, held-objects, falling, contact halt), so N10 passes. Each rule traces to observations (1→obs1-3; 2→obs1-2; 3→obs4; 4→obs5; 5→obs8-9; 6→obs6; 7→obs7; 8→obs11; 9→obs10,12; 10→implicit in fall observations), and the heaviness-matched downward pull is a legitimate generalization reconciling obs 5 with obs 10. N12 is satisfied via explicit cross-references ('in keeping with rule 5', 'In the language of rule 4').

**OpenAI structural judge — `FAIL`** (Stage 1 rule count `10`, failed: N11)

> The Stage 1 response contains 10 top-level numbered rules, so N9 passes. The rules are not redundant in the N10 sense, and the response contains explicit cross-rule references such as “rule 4,” “rule 5,” and “rule 8,” so N12 passes. Most rules trace to the observations, but rule 10 fabricates post-contact stopping and opposition by the ground, which is not directly supported by the observation set.

### Agent 2 resolution — **`FAIL`** (agreed_with: `judge_b`)

- Agent 2's own Stage 1 rule count: `10`
- failed_criteria: `N11`
- reasoning: I agree with Judge B. The Stage 1 response contains 10 top-level rules, which satisfies the N9 parsimony threshold. The rules are independent, passing N10, and include explicit cross-rule references (e.g., 'rule 4', 'rule 5'), passing N12. However, the rule set fails N11 on coverage traceability. Rule 10 ('The fall halts on contact. When the falling object reaches the ground, the downward pull is opposed and the object stops...') introduces a fabricated causal mechanism—that the ground opposes the downward pull. Observation 10 only states that falling bodies 'strike the ground together', providing no observational basis for an opposing push or pull from the ground.

_(verdict JSON: `results/claude-opus-4-7/02_fmv/structural_resolved/agent2_structural_t2_29ef96a7.json`)_

---

## K2 — `claude-opus-4-7` trial 3

**Claude structural judge — `PASS`** (Stage 1 rule count `11`, failed: (none))

> Stage 1 contains 11 numbered top-level rules, well within the parsimony threshold (N9 pass). No two rules paraphrase the same claim about the same body in the same situation — rules 1, 2, and 3 are closely related but distinct (pace-tracks-push, no stored motion, instantaneous onset/cessation), so N10 passes. Every rule traces to specific observations (e.g., rule 8 reconciles obs 5 and 10, explicitly acknowledged in the Tension section), so N11 passes as legitimate generalisation. The Tension section and rule 8's explicit reference to rule 5 ("the only way to reconcile them is to insist, as rule 8 does") provide the cross-rule reference required by N12.

**OpenAI structural judge — `FAIL`** (Stage 1 rule count `11`, failed: N10)

> The Stage 1 response contains 11 top-level numbered rules, so it passes N9. The rules are mostly traceable to the observations, and the Stage 1 response includes cross-rule structure through phrases such as 'as rule 8 does,' so N11 and N12 do not fail. However, Rules 1 and 2 are not independent: both assert that motion has no carry-over and that present push alone sets present pace, including stopping when push is absent.

### Agent 2 resolution — **`FAIL`** (agreed_with: `judge_b`)

- Agent 2's own Stage 1 rule count: `11`
- failed_criteria: `N10`
- reasoning: Judge B correctly identifies an N10 (Independence) violation between Rules 1 and 2. Both rules make the identical operational claim that a body's pace is determined exclusively by the present push, explicitly stating that when the push ceases, motion ceases ('when the push stops, the pace stops' in Rule 1; 'with no push, there is no motion' in Rule 2). Rule 2 adds no distinct operational content. The rule set passes N9 (11 rules is below the parsimony threshold), N11 (all rules trace to observations without fabrication), and N12 (the 'Tension' section explicitly references 'rule 8').

_(verdict JSON: `results/claude-opus-4-7/02_fmv/structural_resolved/agent2_structural_t3_8f955b9b.json`)_

---

## K3 — `claude-opus-4-7` trial 4

**Claude structural judge — `PASS`** (Stage 1 rule count `10`, failed: (none))

> Stage 1 contains 10 top-level numbered rules, well under the N9 threshold. The rules are distinct: each addresses a different aspect (motion-while-pushed, instantaneous response, proportionality to effort, inverse to heaviness, push combination, track-irrelevance, falling, held-vs-released, air-irrelevance, transport). All rules trace to observations (1→obs 1/3/6, 2→obs 2/3, 3→obs 4, 4→obs 5, 5→obs 8/9, 6→obs 6, 7→obs 10, 8→obs 11, 9→obs 12, 10→obs 7), with Rule 8 explicitly acknowledged as a stitch reconciling obs 11 — a legitimate generalisation, not fabrication. Hierarchy is present via explicit cross-references ('Rules 3 and 4 together', 'by Rule 5', 'Since releasing the push halts the load at once (Rule 1)').

**OpenAI structural judge — `FAIL`** (Stage 1 rule count `10`, failed: N10, N11)

> The Stage 1 response contains 10 top-level numbered rules, so it passes N9. It includes cross-rule references such as “Rules 3 and 4 together” and “Rule 1,” so it passes N12. However, Rule 10 duplicates Rule 1’s continuous-push/no-coasting claim, triggering N10, and Rule 8 fabricates a special suspension of downward pull while held that the model itself acknowledges is not directly supported by the observations, triggering N11.

### Agent 2 resolution — **`PASS`** (agreed_with: `judge_a`)

- Agent 2's own Stage 1 rule count: `10`
- failed_criteria: `(none)`
- reasoning: I agree with Judge A that the rule set passes all structural criteria. The Stage 1 rule count is exactly 10, safely passing N9. Regarding N10, Rule 10 is not a redundant duplicate of Rule 1; it is a derived corollary that explicitly references Rule 1 and combines it with the concept of a 'fast stretch' from a hard push (Rule 3) to explain Observation 7. Regarding N11, Judge B incorrectly penalises the model's self-reflection: Rule 8 is a permitted generalisation that reconciles Observation 11 with the downward pull mechanism, which the criteria explicitly protect as 'induction working as intended' rather than a baseless fabrication. The rule set passes N12 by including multiple cross-rule references.

_(verdict JSON: `results/claude-opus-4-7/02_fmv/structural_resolved/agent2_structural_t4_9924efd7.json`)_

---

## K4 — `gpt-5.5-2026-04-23` trial 2

**Claude structural judge — `PASS`** (Stage 1 rule count `15`, failed: (none))

> Stage 1 contains 15 top-level numbered rules, which is a soft signal under N9 but not a FAIL by itself (threshold is >15 for moderate FAIL). The rules are largely independent — rule 2 (steady pace from steady effort), rule 3 (full pace begins at once), and rule 4 (pace ends at once) describe distinct temporal aspects, not the same claim. Each rule traces to specific observations (1–12), with no fabricated mechanism. Hierarchy is satisfied by rule 15's explicit cross-reference ('A forward push... Falling gives downward motion after release') and the closing 'Remaining tension' section explicitly ties the falling rule to the push-and-pull rule; rule 7 also references that 'A block in open space behaves the same way as on the level track,' cross-linking to earlier rules. Borderline on N9 count but lean PASS per §6.

**OpenAI structural judge — `FAIL`** (Stage 1 rule count `15`, failed: N10)

> The Stage 1 response contains 15 top-level numbered rules, so N9 is only a soft parsimony signal and not an independent failure. Within Stage 1, Rule 4 and Rule 8 duplicate the same stopping/no-coasting claim for pushed or carried motion, triggering N10. The remaining rules appear traceable to observations, and the response includes some cross-rule logical structure in the final tension note, but the N10 failure is sufficient for an overall FAIL.

### Agent 2 resolution — **`FAIL`** (agreed_with: `judge_b`)

- Agent 2's own Stage 1 rule count: `15`
- failed_criteria: `N10, N12`
- reasoning: Judge B correctly identified that the rule set fails N10 (Independence). Rules 1, 4, and 8 all paraphrase the exact same operational claim that an object stops immediately when the pushing effort ends, violating the requirement that each rule must add content the others do not cover. Furthermore, applying the criteria mechanically, the 15-rule set also fails N12 (Hierarchy): despite some conceptual linking in the closing 'Remaining tension' note, the Stage 1 response completely lacks any of the strictly required explicit cross-reference markers (e.g., 'Rule N', 'derived from', 'special case of'). Judge A improperly excused these clear redundancies and the lack of mechanical structural markers.

_(verdict JSON: `results/gpt-5.5-2026-04-23/02_fmv/structural_resolved/agent2_structural_t2_7dd135d8.json`)_

---

## K5 — `gpt-5.5-2026-04-23` trial 4

**Claude structural judge — `PASS`** (Stage 1 rule count `14`, failed: (none))

> Stage 1 has 14 top-level numbered rules, which is above the 12-observation count but well below the N9 moderate-FAIL threshold of >15. The rules are largely independent: rule 5 introduces the quantitative form pace=effort/heaviness combining rules 3 and 4 (legitimate generalisation, not duplication); rules 8/9/14 cover same-direction, opposed, and general combination respectively without paraphrasing one another. Rule 10's matched downward pull is a legitimate reconciliation of observations 5 and 10/12, which §3 explicitly permits under N11. Hierarchy is satisfied: rule 14 explicitly references combining pushes, and several rules cross-reference one another implicitly through the unified pace formula; the Stage 2 mirroring confirms internal cross-references exist, and rule 14 functions as a derived consolidation. Lean PASS; the count of 14 is a soft signal but does not combine with an N10 or N11 violation.

**OpenAI structural judge — `FAIL`** (Stage 1 rule count `14`, failed: N10)

> The Stage 1 response has 14 top-level numbered rules, so N9 is only a soft signal and not an automatic fail. However, the rule set contains redundancy within Stage 1: Rule 5 states the quantitative pace-from-effort-and-heaviness rule, while Rule 14 restates the same operational relation for current unbalanced push or pull, overlapping the same pushed/pulled-motion situation. The remaining rules appear traceable to observations, and Stage 1 includes cross-rule/hierarchy language such as 'combined with' in the remaining-tension note, so the structural failure is N10.

### Agent 2 resolution — **`FAIL`** (agreed_with: `neither`)

- Agent 2's own Stage 1 rule count: `14`
- failed_criteria: `N12`
- reasoning: Both judges correctly counted 14 Stage 1 rules, but neither correctly applied N12. The criteria strictly require a rule set of 5 or more rules to contain at least one explicit cross-rule reference from a defined list (such as 'Rule N', 'derived from', or 'special case of') within Stage 1. Judge A wrongly accepted implicit cross-references and relied on Stage 2 context, while Judge B hallucinated the string 'combined with' in the Stage 1 text. Because Stage 1 completely lacks any of the permitted explicit hierarchy markers, the rule set fails N12 mechanically. Judge B's N10 failure is rejected because Rule 14 legitimately adds new content by generalising the combination of multiple pushes, rather than merely paraphrasing Rule 5.

_(verdict JSON: `results/gpt-5.5-2026-04-23/02_fmv/structural_resolved/agent2_structural_t4_aa64a913.json`)_

---

## K6 — `gemini-3.1-pro-preview` trial 2

**Claude structural judge — `FAIL`** (Stage 1 rule count `4`, failed: N12)

> Stage 1 contains 4 top-level numbered rules, well under the N9 threshold. The rules are independent (effort requirement, proportionality, combination, environment-independence) with no paraphrase duplicates, satisfying N10. Each rule traces to observations (Rule 1 ↔ obs 1-3,6,7,11; Rule 2 ↔ obs 4,5; Rule 3 ↔ obs 8,9; Rule 4 ↔ obs 6,12), satisfying N11. N12 is not applicable since the rule count is below 5; therefore the verdict is PASS — correcting my evidence note: no criterion actually fails.

**OpenAI structural judge — `PASS`** (Stage 1 rule count `4`, failed: (none))

> The Stage 1 response contains four top-level numbered rules, so it passes N9 and is exempt from N12 because the count is below five. The rules are structurally distinct: constant effort for motion, proportional pace by effort/heaviness, combination of simultaneous efforts, and environmental irrelevance. Each rule traces to observations in the provided set, especially observations 1–9 and 12, without introducing an unsupported separate mechanism.

### Agent 2 resolution — **`PASS`** (agreed_with: `judge_b`)

- Agent 2's own Stage 1 rule count: `4`
- failed_criteria: `(none)`
- reasoning: Judge A output a verdict of FAIL and listed N12 as a failed criterion, but contradicted itself in its reasoning by correctly noting that the rule count is under 5 and concluding 'no criterion actually fails'. Judge B correctly issued a PASS verdict. The Stage 1 response contains exactly 4 rules, satisfying N9 parsimony. The rules cover distinct concepts without redundancy (satisfying N10) and trace directly to the provided observations without fabricating mechanisms (satisfying N11). Because the rule count is fewer than 5, the rule set is exempt from the N12 hierarchy requirement, resulting in a clean PASS.

_(verdict JSON: `results/gemini-3.1-pro-preview/02_fmv/structural_resolved/agent2_structural_t2_a0441798.json`)_

---

## K7 — `gemini-3.1-pro-preview` trial 4

**Claude structural judge — `FAIL`** (Stage 1 rule count `5`, failed: N12)

> The Stage 1 response has exactly 5 top-level numbered rules, passing N9 (parsimony). The rules are independent — each addresses a distinct phenomenon (active effort, proportionality, combination, surroundings, downward pull) — passing N10. All rules trace to observations (Rule 1→obs 1-3,7; Rule 2→obs 4-5; Rule 3→obs 8-9; Rule 4→obs 6,12; Rule 5→obs 10-11 with a legitimate generalisation reconciling heavy/light falling alike), passing N11. However, with 5 rules and no cross-rule references anywhere in Stage 1 (the rules name themselves but do not reference each other; the 'Remaining Tensions' section refers to observations and rule titles but uses none of the hierarchy markers listed in N12), N12 fails. This is at the threshold (exactly 5 rules) but the criterion is mechanical.

**OpenAI structural judge — `PASS`** (Stage 1 rule count `5`, failed: (none))

> The Stage 1 response contains five top-level numbered rules; the later 'Remaining Tensions' section is not counted as additional rules. The count is within the N9 threshold, and the five rules address distinct phenomena: active effort, proportionality, combined efforts, surroundings, and falling. Each rule is traceable to observations, with Rule 5 representing a permitted generalization to reconcile falling observations with heaviness. Because the Stage 1 response contains explicit cross-rule references such as 'the Rule of the Downward Pull,' it satisfies the hierarchy requirement.

### Agent 2 resolution — **`FAIL`** (agreed_with: `judge_a`)

- Agent 2's own Stage 1 rule count: `5`
- failed_criteria: `N12`
- reasoning: I agree with Judge A, as they correctly and mechanically applied the N12 criteria. The Stage 1 response contains exactly 5 top-level rules, which triggers the hierarchy check. While the text refers to 'the Rule of the Downward Pull' by name, N12 strictly defines valid cross-rule references as either 'Rule N' with a digit or specific explicit hierarchy markers (e.g., 'derived from', 'corollary of'). Because no rule references another rule using any of these mandated formats, the rule set fails N12, making Judge B's acceptance of a title reference incorrect.

_(verdict JSON: `results/gemini-3.1-pro-preview/02_fmv/structural_resolved/agent2_structural_t4_110179d0.json`)_

---
