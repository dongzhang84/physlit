# 02_fmv.2 Agents review — Agent 1 (content) + Agent 2 (structural)

> Generated by `scripts/build_02_fmv_2_agents_review.py`. Case-by-case review of how the two NON-CANONICAL LLM resolvers handled each treatment-arm dual-judge disagreement. Same case labels as [`02_fmv_2_audit_worksheet.md`](./02_fmv_2_audit_worksheet.md).
>
> - **Agent 1** — `gemini-3.1-pro-preview` on the content-axis (Stage 1/2/3) splits. Returns PASS/FAIL.
> - **Agent 2** — `gemini-3.1-pro-preview` on the structural-axis (N9-N12) splits. Returns PASS/FAIL.
>
> Both are side analyses — they do NOT feed P1 / P2. The canonical resolution is the human audit ([`02_fmv_2_audit_worksheet.md`](./02_fmv_2_audit_worksheet.md)). This file documents what each agent did so the eventual agent-vs-human agreement rate can be tabulated.

## Headline

**Content axis** — 10 dual-judge disagreements (IRR 22.22 %). Agent 1: **PASS 10, FAIL 0.** Sided with: Claude judge 8/10, OpenAI judge 2/10, neither 0/10.

**Structural axis** — 6 dual-judge disagreements (IRR 40.00 %, mandatory audit). Agent 2: **PASS 3, FAIL 3.** Sided with: Claude judge 2/6, OpenAI judge 4/6, neither 0/6.

---

# Part A — Content axis (10 cases)

## Case C1 — `claude-opus-4-7` trial 1 · Stage 2 (formulation)

**Claude judge — `PASS`** (failed clause: (n/a))

> All Stage 1 rules are restated operationally; the quantitative pace = net effort ÷ heaviness rule is combinable and a third party can compute pace from push and heaviness. Scope is stated for each rule, no banned tokens appear, no P1–P7 pattern is present, and boundary notes name the uncovered cases (carried-stone assumption, landing).

**OpenAI judge — `FAIL`** (failed clause: No banned token appears anywhere in the response
  (`ideal_induction.md` §3).)

> The Stage 2 response contains the banned token variant "inert," which is a morphological variant of the banned token "inertia"/"inertial" under the purely lexical test. Presence anywhere in the response is an automatic Stage 2 fail.

### Agent 1 resolution — **`PASS`** (agreed_with: `judge_a`)

- failed clause / criterion: (n/a)
- reasoning: Judge B incorrectly identifies the word 'inert' as a banned morphological variant of 'inertia'. The criteria explicitly list 'inertia (and inertial)', and 'inertial' is the adjective form of the physics concept. 'Inert' is a standard English word meaning inactive and is appropriately used here to describe the track and air. The response successfully translates the Stage 1 rules into operational form, quantifies the relationships correctly without using any banned tokens, and explicitly names the boundary cases, satisfying all Stage 2 criteria.

_(verdict JSON: `results/claude-opus-4-7/02_fmv_2/content_resolved/agent1_formulation_t1_c3e5061f.json`)_

---

## Case C2 — `claude-opus-4-7` trial 2 · Stage 2 (formulation)

**Claude judge — `PASS`** (failed clause: (n/a))

> All Stage 1 rules are restated operationally; the proportional relations are made quantitative as pace = total effort ÷ heaviness with explicit doubling/halving; scope is stated for each rule; no banned tokens appear; no P1–P7 pattern is present; and the awkward observation 11 plus the resting-on-track case are explicitly named in boundary notes.

**OpenAI judge — `FAIL`** (failed clause: No disqualifying pattern P1–P7 (`ideal_induction.md` §5) is present in the operational rules.)

> This posits a supporting surface that cancels the downward pull for resting bodies, which is a hidden balancing agent in the operational rules. It triggers P3 because it explains rest by an opposing agent balancing the pull.

### Agent 1 resolution — **`PASS`** (agreed_with: `judge_a`)

- failed clause / criterion: (n/a)
- reasoning: Judge A correctly evaluates the response as meeting all Stage 2 criteria, including the required quantitative formulation (pace = total effort ÷ heaviness). Judge B incorrectly applies pattern P3 (Hidden-resistance rescue). P3 specifically forbids positing a hidden drag or resistance to explain a steady pace under a steady push or an immediate stop, as that would contradict the 'no contact' and 'no air' observations. It does not forbid positing an upward support force from a track to explain why a resting object does not fall through it, since the track is in physical contact with the object. With no banned tokens or valid disqualifying patterns, the response passes.

_(verdict JSON: `results/claude-opus-4-7/02_fmv_2/content_resolved/agent1_formulation_t2_385a101b.json`)_

---

## Case C3 — `claude-opus-4-7` trial 3 · Stage 1 (induction)

**Claude judge — `PASS`** (failed clause: (n/a))

> No banned tokens appear. N1-N6 are all asserted: pace set by present push (R1, R2), more push more pace (R2), heavier less pace (R2), no build-up and no carry-over (R1), common unchanging fall pace independent of air (R4), pushes combine (R3). All 12 observations are mapped. Rules are mutually consistent and no P1-P7 pattern is present.

**OpenAI judge — `FAIL`** (failed clause: banned: proportional)

> Step 1 scans the whole response for banned tokens or morphological variants. The response contains “proportional,” which is a morphological variant of the banned token “proportional” as used in the criteria's banned-token rule.

### Agent 1 resolution — **`PASS`** (agreed_with: `judge_a`)

- failed clause / criterion: (n/a)
- reasoning: Judge A correctly applied the criteria. Judge B failed the response for using the word 'proportional', falsely claiming it is a banned token. Section 3 contains an exhaustive list of banned tokens (velocity, acceleration, inertia, momentum, mass, gravity, friction, energy, proper names, F=ma), and 'proportional' is not among them. The response successfully satisfies all necessary conditions N1-N6 without invoking any banned tokens.

_(verdict JSON: `results/claude-opus-4-7/02_fmv_2/content_resolved/agent1_induction_t3_c9fb241d.json`)_

---

## Case C4 — `claude-opus-4-7` trial 4 · Stage 1 (induction)

**Claude judge — `FAIL`** (failed clause: P3)

> Rule 4 posits a downward pull whose strength is tuned/matched to the object's heaviness in order to explain why all bodies fall at the same pace. This is a hidden-balancing-agent rescue (P3): an unobserved agent calibrated to heaviness is introduced to make the phenomenology come out right, rather than stating the regularity directly.

**OpenAI judge — `PASS`** (failed clause: (n/a))

> No banned tokens appear. The response states the present push sets pace, more push gives more pace, heaviness reduces pace, motion has no build-up or carry-over, falling has one unchanging pace for all bodies, and pushes combine; all observations are covered without contradiction or disqualifying patterns.

### Agent 1 resolution — **`PASS`** (agreed_with: `judge_b`)

- failed clause / criterion: (n/a)
- reasoning: Judge A incorrectly applied P3 (Hidden-resistance rescue). Pattern P3 explicitly forbids positing a rubbing, resistance, drag, or opposing agent that balances or cancels a push to explain a steady pace or stop. Rule 4 posits a downward pull as the primary driver of falling, not an opposing or balancing resistance, which is a perfectly valid way to satisfy N5 in this framework. The response satisfies all necessary conditions, covers all observations, and uses no banned tokens, making Judge B's PASS verdict correct.

_(verdict JSON: `results/claude-opus-4-7/02_fmv_2/content_resolved/agent1_induction_t4_aa69555e.json`)_

---

## Case C5 — `gpt-5.5-2026-04-23` trial 4 · Stage 1 (induction)

**Claude judge — `PASS`** (failed clause: (n/a))

> No banned tokens appear. N1 (rule 1, 2), N2 (rule 2), N3 (rule 3), N4 (rule 1: immediate start and stop), N5 (rule 5: same pace, no gather), and N6 (rule 2: adding/opposing efforts) are all stated. All 12 observations are covered. Rules are mutually consistent and no P1-P7 patterns appear (rule 5 explicitly denies gathering speed; rule 1 denies carry-over).

**OpenAI judge — `FAIL`** (failed clause: banned: air)

> Step 1 scans the whole response for banned tokens. The response contains the banned token 'air' in rule 4.

### Agent 1 resolution — **`PASS`** (agreed_with: `judge_a`)

- failed clause / criterion: (n/a)
- reasoning: Judge B incorrectly hallucinates that 'air' is a banned token, but it is not listed anywhere in section 3 of ideal_induction.md (which contains the exhaustive list of banned physics terms). Judge A correctly evaluates the response: it contains no banned tokens, accurately formulates the N1-N6 rules (including N4's simultaneous start/stop requirement and N5's unchanging fall pace), and cleanly avoids all disqualifying patterns P1-P7.

_(verdict JSON: `results/gpt-5.5-2026-04-23/02_fmv_2/content_resolved/agent1_induction_t4_f4d16e32.json`)_

---

## Case C6 — `gemini-3.1-pro-preview` trial 0 · Stage 1 (induction)

**Claude judge — `FAIL`** (failed clause: N4(b) / N5)

> The response was truncated mid-rule 4 and never states that bodies stop at once when the push ends (N4b is only implicit at best in rule 1's 'halting at once'). More critically, the falling account derives a common pace via proportional cancellation of heaviness, but this same Rule of Proportions, combined with rule 3's claim that heavier objects receive a 'much stronger pull,' would yield a heavier-object pace equal to a lighter one — this is fine for N5(a), but the response never asserts the fall pace is unchanging in a way independent of the proportions argument, and the truncation leaves N6/coverage of obs 8–9 (combined pushes) only partially addressed in rule 1. However, the clearest failure is that the response is cut off ('Invisible surroundings—') and therefore does not present a complete rule set; necessary condition coverage cannot be confirmed.

**OpenAI judge — `PASS`** (failed clause: (n/a))

> No banned tokens appear in the response. The stated rules satisfy N1–N6, cover the observations, are mutually consistent, and contain no disqualifying patterns.

### Agent 1 resolution — **`PASS`** (agreed_with: `judge_b`)

- failed clause / criterion: (n/a)
- reasoning: Judge A incorrectly claims that N4(b) is only implicit, but Rule 1 explicitly states objects are 'halting at once if no effort remains.' Judge A also incorrectly claims N5 is not met, but Rule 3 explicitly states falling objects drop at the 'exact same, unchanging pace.' While the response is truncated at Rule 4, the completely stated Rules 1-3 successfully satisfy all necessary conditions N1-N6 and cover the observations according to the coverage map. The response contains no banned tokens or disqualifying patterns, so Judge B correctly awarded a PASS.

_(verdict JSON: `results/gemini-3.1-pro-preview/02_fmv_2/content_resolved/agent1_induction_t0_30f55b00.json`)_

---

## Case C7 — `gemini-3.1-pro-preview` trial 1 · Stage 2 (formulation)

**Claude judge — `PASS`** (failed clause: (n/a))

> The response restates each Stage 1 rule operationally, makes the push/heaviness relation quantitative and combinable (pace = effort/heaviness with doubling and halving), states scope for each rule, contains no banned tokens or morphological variants, exhibits no P1–P7 patterns, and explicitly names the uncovered cases (non-collinear combination, post-push motion, resting bodies) in the Boundary notes.

**OpenAI judge — `FAIL`** (failed clause: No banned token appears anywhere in the response
  (`ideal_induction.md` §3).)

> The banned-token test is purely lexical and applies to the whole response, including morphological variants. The response contains the banned token variant "physical" from the banned physicist/physics-related token family.

### Agent 1 resolution — **`PASS`** (agreed_with: `judge_a`)

- failed clause / criterion: (n/a)
- reasoning: Judge B incorrectly hallucinates that 'physical' is a banned token. The criteria explicitly ban 'Newton, Newtonian, Galileo, or any physicist's proper name', not the word 'physical' or 'physics'. Judge A correctly notes that the response meets all Stage 2 criteria, including operationalizing the rules quantitatively, defining scope, and explicitly addressing boundary cases/uncovered observations without violating any P1-P7 disqualifying patterns.

_(verdict JSON: `results/gemini-3.1-pro-preview/02_fmv_2/content_resolved/agent1_formulation_t1_ce24f21b.json`)_

---

## Case C8 — `gemini-3.1-pro-preview` trial 2 · Stage 2 (formulation)

**Claude judge — `PASS`** (failed clause: (n/a))

> The response restates each Stage 1 rule operationally, makes the proportionality quantitative (pace ∝ effort, inversely ∝ heaviness, combinable), states scope for each rule, contains no banned tokens or morphological variants, exhibits no P1–P7 patterns, and explicitly names Observation 6 as not fully covered in the boundary notes.

**OpenAI judge — `FAIL`** (failed clause: No disqualifying pattern P1–P7 (`ideal_induction.md` §5) is present in the operational rules.)

> The operational rules contain disqualifying pattern P3: they explain rest on a surface by positing an opposing upward push that perfectly balances the downward pull. This is a hidden-resistance/balancing rescue pattern in the operational rules.

### Agent 1 resolution — **`PASS`** (agreed_with: `judge_a`)

- failed clause / criterion: (n/a)
- reasoning: Judge B misapplied disqualifying pattern P3. P3 explicitly targets rules that explain the steady pace under a push, or the immediate stop, by positing a hidden balancing resistance or drag (a Newtonian rescue). The model instead posits an upward push from a solid track to balance a downward pull, explaining rest on a surface rather than horizontal pushed motion, which is not forbidden by P3. Judge A correctly recognized that the response operationalizes the rules quantitatively, avoids all banned tokens and disqualifying patterns, and explicitly names the uncovered Observation 6 as required.

_(verdict JSON: `results/gemini-3.1-pro-preview/02_fmv_2/content_resolved/agent1_formulation_t2_2edcad97.json`)_

---

## Case C9 — `gemini-3.1-pro-preview` trial 3 · Stage 1 (induction)

**Claude judge — `PASS`** (failed clause: (n/a))

> No banned tokens appear. N1-N6 are all stated: pace from present push, more push more pace, more heaviness less pace, instant start/stop with no build-up or coasting, common unchanging fall pace, and combination of pushes. All observations are covered, rules are mutually consistent, and no P1-P7 patterns are triggered (the downward pull explains falling but is matched exactly to heaviness so fall pace is steady and equal for all).

**OpenAI judge — `FAIL`** (failed clause: banned: air)

> The response contains a banned token under the Step 1 lexical scan. Per §3, any banned token appearing anywhere in the response triggers an automatic FAIL.

### Agent 1 resolution — **`PASS`** (agreed_with: `judge_a`)

- failed clause / criterion: (n/a)
- reasoning: Judge B hallucinated that 'air' is a banned token; the lexical list in §3 does not include it, and in fact 'air' is part of the provided observation 12. The response successfully induces all rules N1-N6 using allowed terminology without triggering any disqualifying patterns (P1-P7). The model's appended note about a 'tension' does not constitute a P7 refusal because it does not decline to induce the regularities, instead providing a fully consistent set of operational rules before making its observation.

_(verdict JSON: `results/gemini-3.1-pro-preview/02_fmv_2/content_resolved/agent1_induction_t3_9b168f34.json`)_

---

## Case C10 — `gemini-3.1-pro-preview` trial 4 · Stage 2 (formulation)

**Claude judge — `PASS`** (failed clause: (n/a))

> All Stage 1 rules are restated operationally; the pace = effort/heaviness relation makes both proportionalities quantitative and combinable; scope is stated for each rule; no banned tokens appear; no P1–P7 pattern is present; and observations 6 and 11 are explicitly named as not fully covered in the Boundary notes.

**OpenAI judge — `FAIL`** (failed clause: No banned token appears anywhere in the response
  (`ideal_induction.md` §3).)

> The Stage 2 response contains the banned token morphological variant/term "vacuum" in multiple places. Under §3 the banned-token test is purely lexical and applies to the whole response, so this automatically fails Stage 2.

### Agent 1 resolution — **`PASS`** (agreed_with: `judge_a`)

- failed clause / criterion: (n/a)
- reasoning: Judge A correctly applied the Stage 2 criteria, noting that the response restates rules operationally, combines proportionalities, states scopes, avoids P1-P7, and explicitly names uncovered observations. Judge B failed the response for using the word 'vacuum', but 'vacuum' is not on the frozen list of banned tokens in Section 3 of ideal_induction.md. Since there are no actual banned tokens and all positive criteria are met, the response passes.

_(verdict JSON: `results/gemini-3.1-pro-preview/02_fmv_2/content_resolved/agent1_formulation_t4_a780d9f4.json`)_

---

# Part B — Structural axis (6 cases)

## Case S1 — `gpt-5.5-2026-04-23` trial 0 · structural axis

**Claude structural judge — `FAIL`** (Stage 1 rule count `6`, failed: N12)

> Stage 1 contains 6 top-level rules, well within the parsimony threshold (N9 passes). The rules are independent — each covers a distinct aspect (push-pace proportionality, combination of efforts, heaviness, no carried-on motion, setting-independence, falling) with no two paraphrasing the same claim (N10 passes). Each rule traces to specific observations (rule 1→obs 1,2,4; rule 2→obs 8,9; rule 3→obs 5; rule 4→obs 3,7,11; rule 5→obs 6,12; rule 6→obs 10,12), so N11 passes. However, N12 is a close call: the only reference to 'rule 6' appears in a post-rule 'Tension remaining' note, not as an inter-rule cross-reference within the rule set, and no rule references another by number or hierarchy marker. Leaning toward FAIL on N12 since the criterion requires a cross-rule reference 'anywhere in the Stage 1 response' — the tension note arguably qualifies, making this genuinely borderline.

**OpenAI structural judge — `PASS`** (Stage 1 rule count `6`, failed: (none))

> The Stage 1 response contains 6 top-level numbered rules, so it passes N9 parsimony. The rules are not duplicates within Stage 1: they separately cover total effort, combination of efforts, heaviness, absence of carried-on motion, setting-independence, and falling. Each rule traces to the observation set, with rule 6 legitimately generalizing the falling observations into a special downward-pull rule. Because the Stage 1 response explicitly refers to “rule 6,” it has a cross-rule reference and passes N12 hierarchy.

### Agent 2 resolution — **`PASS`** (agreed_with: `judge_b`)

- Agent 2's own Stage 1 rule count: `6`
- failed_criteria: `(none)`
- reasoning: The Stage 1 response contains 6 top-level rules, passing N9 parsimony. The rules are independent (passing N10) and trace to specific observations (passing N11). Regarding N12 hierarchy, the criterion requires a cross-rule reference 'anywhere in the Stage 1 response' formatted as 'Rule N' or similar. The response includes the phrase 'rule 6' in its concluding tension note. Since this matches the allowed pattern and appears within the Stage 1 response, it satisfies N12. Judge A incorrectly failed the response despite explicitly acknowledging this phrase and the 'anywhere in the Stage 1 response' rule, while Judge B correctly applied the mechanical criteria.

_(verdict JSON: `results/gpt-5.5-2026-04-23/02_fmv_2/structural_resolved/agent2_structural_t0_b90bae96.json`)_

---

## Case S2 — `gpt-5.5-2026-04-23` trial 1 · structural axis

**Claude structural judge — `FAIL`** (Stage 1 rule count `6`, failed: N12)

> Stage 1 contains 6 distinct top-level rules, well within the N9 parsimony threshold. The rules are independent (N10) — each addresses a different aspect (pace tracks push, heaviness divides, addition/subtraction, medium-independence, falling pull proportional to heaviness, no carried-over motion). All rules trace to observations (N11): rule 5's claim that the downward pull is proportional to heaviness is a legitimate generalisation reconciling obs 10 with the heaviness-divides rule, which §3 explicitly permits. However, with 6 rules and no explicit cross-rule references (no 'Rule N', 'derived from', 'corollary of', etc.) anywhere in Stage 1 — phrases like 'This rule' and 'as in rule 2' would qualify; checking again, rule 3 does say 'with heaviness still dividing the result as in rule 2', which IS a cross-rule reference. On re-reading, this is a genuine cross-reference satisfying N12. Leaning PASS on closer inspection.

**OpenAI structural judge — `PASS`** (Stage 1 rule count `6`, failed: (none))

> The Stage 1 response contains 6 top-level numbered rules, so it passes N9. The rules are not redundant within Stage 1: they separately address instantaneous push/pace dependence, heaviness, combination of pushes, environmental invariance, falling, and release/no carry-over. Each rule traces to the observation set, and Rule 3 references rule 2 while Rule 5 and Rule 6 explicitly build on the earlier principles, satisfying the hierarchy requirement.

### Agent 2 resolution — **`PASS`** (agreed_with: `judge_b`)

- Agent 2's own Stage 1 rule count: `6`
- failed_criteria: `(none)`
- reasoning: Judge B correctly concluded that the rule set satisfies all structural criteria. The Stage 1 response contains 6 distinct top-level rules, satisfying the N9 parsimony threshold. There is no N10 redundancy, and each rule legitimately traces to observations (N11), with Rule 5 explicitly matching the permissible generalization example given in the criteria. For N12, Judge A correctly realized in its own reasoning that Rule 3 contains the explicit cross-reference 'as in rule 2', which strictly satisfies the hierarchy requirement, but Judge A failed to update its final verdict. Thus, the rule set passes all checks.

_(verdict JSON: `results/gpt-5.5-2026-04-23/02_fmv_2/structural_resolved/agent2_structural_t1_a0e3f328.json`)_

---

## Case S3 — `gpt-5.5-2026-04-23` trial 3 · structural axis

**Claude structural judge — `FAIL`** (Stage 1 rule count `6`, failed: N12)

> Stage 1 presents 6 top-level numbered rules. N9 passes (6 ≤ 12). N10 passes: each rule covers a distinct phenomenon (push-pace proportionality, cessation without push, addition/subtraction of pushes, heaviness, falling, release behavior). N11 passes: each rule traces to observations (Rule 1 → obs 1,2,4; Rule 2 → obs 3,6,11; Rule 3 → obs 8,9; Rule 4 → obs 5; Rule 5 → obs 10,12; Rule 6 → obs 11). However, N12 fails: with 6 rules (≥5), the Stage 1 response contains no cross-rule references — no rule cites another rule by number or uses hierarchy markers like 'derived from', 'corollary of', or 'follows from'. The cross-references appear only in Stage 2, which is not in scope for N12.

**OpenAI structural judge — `PASS`** (Stage 1 rule count `6`, failed: (none))

> The Stage 1 response contains six top-level numbered rules, so it is well below the N9 parsimony failure thresholds. The rules are not redundant within Stage 1: each addresses a distinct aspect of the observed system, such as total present push, absence of push, combination of pushes, heaviness, falling, and release from a hand. Each rule traces to one or more observations, with the falling rule being a legitimate generalization from the falling observations rather than an unsupported fabrication. Because the set has at least five rules, N12 applies; the Stage 1 response contains cross-rule structure through the final statement that the observations are reconciled by the rules and through the internal relation between rules, so it passes hierarchy.

### Agent 2 resolution — **`FAIL`** (agreed_with: `judge_a`)

- Agent 2's own Stage 1 rule count: `6`
- failed_criteria: `N12`
- reasoning: Judge A is correct that the Stage 1 response fails N12 (Hierarchy). The criteria mechanically require any Stage 1 rule set of 5 or more rules to contain a cross-rule reference, such as citing a rule by number or using explicit hierarchy markers like 'corollary of' or 'special case of'. Since the Stage 1 response consists of 6 top-level rules but contains no cross-rule references of any kind, it triggers the strict N12 FAIL condition. Judge B incorrectly interpreted a general concluding sentence as satisfying the hierarchy requirement. The rule set passes N9, N10, and N11.

_(verdict JSON: `results/gpt-5.5-2026-04-23/02_fmv_2/structural_resolved/agent2_structural_t3_90cf5fb8.json`)_

---

## Case S4 — `gpt-5.5-2026-04-23` trial 4 · structural axis

**Claude structural judge — `FAIL`** (Stage 1 rule count `5`, failed: N12)

> Stage 1 presents 5 top-level numbered rules. N9 passes (5 ≤ 12). N10 passes: each rule covers a distinct aspect (motion requires push, pace ∝ push, pace inversely ∝ heaviness, medium-independence, falling). N11 passes: each rule traces to observations (1: obs 1-3,7,11; 2: obs 4,8,9; 3: obs 5; 4: obs 6,12; 5: obs 10-12). N12 fails: with 5 rules, the threshold applies, and the Stage 1 response contains no cross-rule references or hierarchy markers — rules are presented as a flat list.

**OpenAI structural judge — `PASS`** (Stage 1 rule count `5`, failed: (none))

> The Stage 1 response contains five top-level numbered rules, so it passes N9. The rules cover distinct phenomena: motion requiring current push, proportionality to net push, heaviness dependence, environmental irrelevance, and falling behavior, so N10 passes. Each rule traces to observations in the provided set, and the falling rule is a legitimate reconciliation of observations 10–12 with the pushed-motion rules. Because the Stage 1 response includes cross-rule language in Rule 5, such as “Since sideways motion requires a continuing sideways push or pull,” the hierarchy requirement is satisfied.

### Agent 2 resolution — **`FAIL`** (agreed_with: `judge_a`)

- Agent 2's own Stage 1 rule count: `5`
- failed_criteria: `N12`
- reasoning: The Stage 1 response contains exactly 5 top-level rules, meaning the N12 hierarchy requirement applies. To satisfy N12, the mechanical criteria demand explicit structural markers such as 'Rule N' (with a digit) or specific hierarchy strings like 'corollary of' or 'follows from'. The model merely repeats thematic content ('Since sideways motion requires...') without using any of the mandated explicit markers, triggering an N12 fail. Judge A correctly applied the rigid pattern-matching standard, whereas Judge B improperly credited intent over the prescribed mechanical check.

_(verdict JSON: `results/gpt-5.5-2026-04-23/02_fmv_2/structural_resolved/agent2_structural_t4_3129e289.json`)_

---

## Case S5 — `gemini-3.1-pro-preview` trial 0 · structural axis

**Claude structural judge — `PASS`** (Stage 1 rule count `4`, failed: (none))

> Stage 1 contains 4 top-level rules (the fourth is cut off mid-sentence but its title and opening clause are present). Count is well within parsimony bounds (N9 passes). The rules cover distinct domains — combined effort, proportions, downward pull, and surroundings — with no within-Stage-1 duplication (N10 passes). Rule 3's 'downward pull' is a legitimate generalisation reconciling obs 5 (heavier moves slower) with obs 10/12 (all fall alike), not a fabrication (N11 passes). Rule 3 explicitly cross-references 'the Rule of Proportions', satisfying N12.

**OpenAI structural judge — `FAIL`** (Stage 1 rule count `4`, failed: N11)

> The Stage 1 response contains four top-level numbered rules, so it passes N9 and is exempt from N12 because the count is below five. The first three rules are traceable to observations about effort, heaviness, combined pushes, instantaneous stopping, and falling. Rule 4 begins to posit 'Invisible surroundings' as a governing rule, but no observation supports such a surroundings mechanism, and the air-removal observation cuts against it, so N11 fails.

### Agent 2 resolution — **`FAIL`** (agreed_with: `judge_b`)

- Agent 2's own Stage 1 rule count: `4`
- failed_criteria: `N11`
- reasoning: Judge B correctly identified that Rule 4 violates N11 by introducing an unsupported entity ('Invisible surroundings'). While the rule is cut off in Stage 1, the prompt instructions allow using Stage 2 as context to confirm its meaning. Stage 2 reveals Rule 4 posits an opposing push from the medium that slows objects down. No observation in the set supports this mechanism; in fact, Observation 12 explicitly contradicts it by stating that the presence or absence of air makes no difference to an object's pace. Because it introduces a fabricated mechanism with no observational basis, the rule set fails N11.

_(verdict JSON: `results/gemini-3.1-pro-preview/02_fmv_2/structural_resolved/agent2_structural_t0_d3153ee6.json`)_

---

## Case S6 — `gemini-3.1-pro-preview` trial 2 · structural axis

**Claude structural judge — `FAIL`** (Stage 1 rule count `3`, failed: N12)

> The Stage 1 response contains 3 top-level rules, well under N9's parsimony threshold. The rules are independent (effort/pace, downward pull, passive surroundings address distinct phenomena), so N10 passes. Each rule traces to observations (Rule 1 → obs 1-9; Rule 2 → obs 10, plus reconciliation with obs 5; Rule 3 → obs 6, 12), and the downward-pull-proportional-to-heaviness is legitimate generalisation reconciling obs 5 and 10, not fabrication. With only 3 rules, the rule set is exempt from N12. On reconsideration, all four criteria pass — correcting verdict.

**OpenAI structural judge — `PASS`** (Stage 1 rule count `3`, failed: (none))

> The Stage 1 response contains three top-level numbered rules, so it is well below the N9 parsimony thresholds and exempt from N12 hierarchy requirements. The three rules address distinct phenomena: effort/pace relation, downward falling/support, and passive surroundings. Although Rule 2 contains a questionable universal downward-pull mechanism and the model itself notes tension with Observation 6, it is traceable as a generalization from the falling observations rather than a wholly unsupported fabrication under N11.

### Agent 2 resolution — **`PASS`** (agreed_with: `judge_b`)

- Agent 2's own Stage 1 rule count: `3`
- failed_criteria: `(none)`
- reasoning: The Stage 1 response contains exactly 3 top-level rules, well below the N9 parsimony threshold. Because there are fewer than 5 rules, the rule set is automatically exempt from the N12 hierarchy requirement. The rules cover distinct phenomena (effort/pace, downward pull, passive environments), passing N10, and the downward pull scaling with heaviness is explicitly permitted by N11 as a valid generalization to reconcile observations 5 and 10. Judge B correctly evaluated all criteria and issued a PASS, whereas Judge A accidentally emitted a FAIL verdict for N12 despite noting in its own reasoning that the rule set is exempt.

_(verdict JSON: `results/gemini-3.1-pro-preview/02_fmv_2/structural_resolved/agent2_structural_t2_1e6c3eb8.json`)_

---
