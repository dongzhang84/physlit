# dependencies (Node)
node_modules/

# next.js
.next/
out/
dist/
*.tsbuildinfo
next-env.d.ts

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
build/

# env (never commit — contains secrets)
.env
.env.local
.env.*.local

# Claude Code local state (scheduler lockfile, etc.)
.claude/

# vercel
.vercel

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# editor
.vscode/
.idea/
*.swp
*.swo
*~

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# misc
Thumbs.db
coverage/

# Structural-audit (Agent 2) raw run output. Per-trial JSONs are cheap to
# regenerate via `scripts/run_structural_audit.py`; committed calibration
# results live in `analysis/v0_2_agent2_calibration.md` (Step 4 deliverable).
analysis/structural_audit/

# Local-only analysis drafts (zh blog + X post). Marked .zh.md and kept
# uncommitted per author preference; promote to repo if/when published.
analysis/*.zh.md
