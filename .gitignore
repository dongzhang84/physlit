# dependencies (Node)
node_modules/

# next.js
.next/
out/
dist/
*.tsbuildinfo
next-env.d.ts

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
build/

# env (never commit — contains secrets)
.env
.env.local
.env.*.local

# Claude Code local state (scheduler lockfile, etc.)
.claude/

# vercel
.vercel

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# editor
.vscode/
.idea/
*.swp
*.swo
*~

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# misc
Thumbs.db
coverage/

# Local-only analysis drafts (zh blog + X post). Marked .zh.md / .zh.txt
# and kept uncommitted per author preference; promote to repo if/when
# published.
analysis/**/*.zh.md
analysis/**/*.zh.txt

# LaTeX paper drafts — local only, do not push to GitHub.
# Markdown source of truth stays at docs/paper_draft.zh.md.
latex_cn/
latex_en/
